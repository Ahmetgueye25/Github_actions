name: Nom du repository

on:
  push:
    paths:
      - .github/workflows/workflow11.yml
jobs:
  afficher_repo:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher le repository
        run: echo "${{ github.repository }}"
