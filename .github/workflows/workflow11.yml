name: Infos sur le context

on:
  push:
    paths:
      - .github/workflows/workflow11.yml
jobs:
  afficher_repo:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher le numéro
        run: echo "Exécution n° : ${{ github.run_number }}"- name: Afficher le repository
      - name: Afficher le numéro
        run: echo "Exécution n° : ${{ github.run_number }}"
