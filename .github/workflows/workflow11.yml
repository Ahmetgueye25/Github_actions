name: workflow sur les conditions de réussite

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: step 1
        run: echo step 1

      - name: step 2
        if: success()
        run: echo "step 2"

      - name: step 3
        run: echo step 3
