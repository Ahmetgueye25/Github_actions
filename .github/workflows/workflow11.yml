name: workflow sur les conditions de réussite

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: step 1
        run: ls fail

      - name: step 2
        if: failure()
        run: echo "Erreur détectée"
